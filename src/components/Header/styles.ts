import styled from 'styled-components';

export const Container = styled.div`
  background: var(--blue);

  display: flex;
  align-items: center;
  padding: 2rem;

  h1 {
    text-align: center;
  }
`;
